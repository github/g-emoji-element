export declare function applyTone(sequence: string, tone: number): string;
export declare function applyTones(sequence: string, tones: number[]): string;
export declare function removeTone(emoji: string): string;
